{
  "name": "crx-001",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite -c vite.popup.config.ts",
    "build": "vue-tsc && vite build -c vite.popup.config.ts",
    "dev-content": "vite -c vite.content.config.ts",
    "build-content": "vue-tsc && vite build -c vite.content.config.ts",
    "dev-background": "vite -c vite.background.config.ts",
    "build-background": "vue-tsc && vite build -c vite.background.config.ts",
    "build-all": "tsc && node scripts/build.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/node": "^20.10.5",
    "vue": "^3.3.11"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vue-tsc": "^1.8.25"
  }
}
